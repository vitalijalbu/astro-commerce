---
/**
 * Clears the session and returns home. GET is fine for a template; production
 * should prefer a POST + CSRF token.
 */
import { logout } from '@lib/auth'

logout(Astro)
return Astro.redirect('/')
---
